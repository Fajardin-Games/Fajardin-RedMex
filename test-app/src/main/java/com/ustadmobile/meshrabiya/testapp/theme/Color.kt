package com.ustadmobile.meshrabiya.testapp.theme

import androidx.compose.ui.graphics.Color

// Meshrabiya palette
val MeshBackground    = Color(0xFF0F1115) // Fondo Principal — casi negro OLED
val MeshAmber         = Color(0xFFFFB703) // Color Primario — ámbar, luz de atardecer
val MeshWood          = Color(0xFF3A2E2A) // Secundario — Madera Oscura
val MeshTeal          = Color(0xFF2A9D8F) // Acento — confirmación / éxito
val MeshText          = Color(0xFFE0E1DD) // Texto Principal — gris claro

// Derived
val MeshOnAmber       = Color(0xFF1A1000) // Texto sobre fondo ámbar
val MeshSurface       = Color(0xFF1A1D21) // Superficie — ligeramente más claro que fondo
val MeshSurfaceVariant= Color(0xFF2C2823) // Variante de superficie — madera muy oscura
val MeshTextSecondary = Color(0xFF9A9891) // Texto secundario / subtítulos
val MeshOutline       = Color(0xFF504540) // Bordes y divisores
val MeshError         = Color(0xFFCF6679) // Errores
